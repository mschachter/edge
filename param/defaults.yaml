run_dir: 'runs'

data_dir: 'data'
data_file: 'text/alice_in_wonderland.txt'

n_unit: 300

n_prop: 20
n_batch: 100
n_train_steps: 7001 # number of forward prop steps

train_state_reset_rate: 6000 # in input steps

grad_clip_norm: 1.0

opt_algorithm: adam
