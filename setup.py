from setuptools import setup, find_packages

setup(
    name='edge',
    version = '.0001',

    # Choose your license
    license='MIT',
    packages=find_packages(),
    install_requires=['tensorflow'],

)
